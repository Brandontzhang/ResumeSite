const initialState = {
    current: true
}

const introReducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
            return state
    }
}

export default introReducer